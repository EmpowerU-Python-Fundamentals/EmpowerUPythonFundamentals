def largest(a: float, b: float) -> float:
    """This function returns the largest number of two numbers"""
    if a > b:   # Порівнюємо два числа та повертаємо найбільше з них
        return a
    else:
        return b
