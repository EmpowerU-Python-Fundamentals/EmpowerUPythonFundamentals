def find_largest(a: float, b: float) -> float:
    """
    Returns the largest of two numbers.

    Parameters:
    a (float): The first number.
    b (float): The second number.

    Returns:
    float: The larger of the two numbers.
    """
    return a if a > b else b