# We need a function that can transform a number (integer) into a string.

def number_to_string(num):
    return str(num)

print(f"{number_to_string(123) = }")
print(f"{number_to_string(999) = }")
print(f"{number_to_string(-100) = }")