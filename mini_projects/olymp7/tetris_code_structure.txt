
🧩 Опис класів та функцій Tetris (Pygame)

🔹 Клас Tetromino
Представляє окрему тетріс-фігуру.

Атрибути:
- shape — форма фігури.
- x, y — позиція фігури на полі.
- rotation — стан обертання.
- color — колір фігури.

Методи:
- cells() → повертає координати блоків фігури.
- rotate(dir=1) → обертає фігуру.

---------------------------------------------------
🔹 Клас Board
Відповідає за ігрове поле.

Атрибути:
- w, h — ширина та висота.
- grid — сітка поля.
- current — поточна фігура.
- next_queue — черга наступних фігур.
- hold_piece — збережена фігура.
- can_hold — чи можна використати HOLD.

Методи:
- inside(x, y) → перевірка меж поля.
- empty(x, y) → чи вільна клітинка.
- valid(tet) → чи можна поставити фігуру.
- lock(tet) → зафіксувати фігуру.
- clear_lines() → видалити повні рядки.
- spawn() → створити нову фігуру.
- hold() → механіка HOLD.

---------------------------------------------------
🔹 Клас Tetris
Основний ігровий процес.

Атрибути:
- board — ігрове поле.
- score — рахунок.
- level — рівень.
- lines_cleared — кількість очищених ліній.
- game_over — стан гри.

Методи:
- step(...) → рух, обертання, дроп.
- tick() → падіння фігури.
- add_score(lines) → додати очки.
- reset() → перезапуск гри.

---------------------------------------------------
🔹 Функції малювання
- draw_board(screen, board) → малює сітку та блоки.
- draw_tetromino(screen, tet) → малює активну фігуру.
- draw_ui(screen, game) → малює рахунок, рівень, NEXT, HOLD.

---------------------------------------------------
🔹 Основний цикл main()
- Обробка подій.
- Виклик логіки гри.
- Малювання кадру.
- Перевірка стану гри.
